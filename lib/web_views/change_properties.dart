import 'package:flutter/material.dart';

class ChangeProperties extends StatelessWidget {
  const ChangeProperties({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
