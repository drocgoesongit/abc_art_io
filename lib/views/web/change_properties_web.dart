import 'package:flutter/material.dart';

class ChangePropertiesWeb extends StatelessWidget {
  const ChangePropertiesWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(children: [
        // Properties section


        // Image section
        
      ],)
    );
  }
}