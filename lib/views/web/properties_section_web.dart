import 'package:flutter/material.dart';

class PropertiesSectionWeb extends StatelessWidget {
  const PropertiesSectionWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}